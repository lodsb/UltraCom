package ui.persistence

import java.io._

import ui._
import ui.paths._

object ProjectFileLoader {

  def load() = {
    /*al project = scala.xml.XML.loadFile("project.xml")
    val paths = (project \\ "path").map {path => {
        val factoryType = (path \ "@factory").text
        val timeNodes = (path \\ "node").map {timeNode => {
          }
        }
        val timeConnections = (path \\ "connection").map {timeConnection => {
          }
        }
        val connections = (path \\ "connection").map {connection => {
          }
        }
        //Path()
      }
    }*/
  }

}
